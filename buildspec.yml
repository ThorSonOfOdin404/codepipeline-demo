version: 0.2

phases:
  build:
    commands:
      - echo Initiating build process
      - mvn clean package
  post_build:
    commands:
      - echo Build completed successfully...

artifacts:
  files:
    - target/SimpleCrudApp-0.0.1.jar
  name: crudApp.jar
  discard-paths: yes
